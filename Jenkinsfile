pipeline {
   agent any

   stages {
      stage('Start') {
         steps {
            echo 'Hello World'
         }
      }
      stage('msi-install') {
         steps {
            bat "java -version"
         }  
      }
   }
}
